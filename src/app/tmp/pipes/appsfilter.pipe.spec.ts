import { AppsfilterPipe } from './appsfilter.pipe';

describe('AppsfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AppsfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
